-------------TO INSTALL SQLAlCHEMY ------------------------------------------------
    pip install sqlalchemy (or) pip install sqlalchemy==2.0.0(latest version)
    pip install psycopg2-binary

to check installation
    pip show sqlalchemy
    (or)
    import sqlalchemy
    print(sqlalchemy.__version__)

-------------TO INSTALL POSTGRESQL-----------------------------------------------------

    sudo apt updatesudo apt install postgresql

to check
    psql --version

Step 4: Start PostgreSQL service
    sudo systemctl start postgresql

Enable auto-start:
    sudo systemctl enable postgresql

Step 5: Check service status
    sudo systemctl status postgresql

Should show:

    active (running)


----------------TO INSTALL pgAdmin---------------------------------------------------------

- sudo apt install curl

# Install the public key for the repository (if not done previously):
curl -fsS https://www.pgadmin.org/static/packages_pgadmin_org.pub | sudo gpg --dearmor -o /usr/share/keyrings/packages-pgadmin-org.gpg

# Create the repository configuration file:
sudo sh -c 'echo "deb [signed-by=/usr/share/keyrings/packages-pgadmin-org.gpg] https://ftp.postgresql.org/pub/pgadmin/pgadmin4/apt/$(lsb_release -cs) pgadmin4 main" > /etc/apt/sources.list.d/pgadmin4.list && apt update'

# Install for both desktop and web modes:
sudo apt install pgadmin4

# to set password
sudo -u postgres psql
alter user postgres with password 'password';

#open pgAdmin
servers-->Register-->server-->In general add name as (LocalConnection)-->In connection add hostaname(localhost) and add pw and save

LocalConnection-->Databases-->schemas-->public-->Tables-->right click open query tool 